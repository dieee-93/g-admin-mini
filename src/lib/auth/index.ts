/**
 * Auth utilities for user context and audit trail
 *
 * @module lib/auth
 */

export { getUserContext, getUserContextSafe } from './userContext';
export type { UserContext } from './userContext';
