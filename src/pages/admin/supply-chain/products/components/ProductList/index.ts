// Export virtualized ProductList (auto-switches based on item count)
export { ProductListVirtualized as ProductList } from './ProductListVirtualized';

// Regular version (for reference or manual use)
export { ProductListNew } from './ProductListNew';

// Legacy export (for backwards compatibility)
export { ProductList as ProductListLegacy } from './ProductList';
